import React from 'react';
import './App.css';
import {BrowserRouter} from 'react-router-dom'

const Page = (props) => {
  return (
    <BrowserRouter>
    <div>Opa, Funcionando...</div>
    </BrowserRouter>
  );
}

export default Page;
